using UnityEngine;
using System.Collections;

public class saveGame : MonoBehaviour
{		
    	//void saveGame(){
		//save level
       	 	//int level = StaticSingleton.instance.getLevel();
        	//PlayerPrefs.SetInt("Save", level);
        	//PlayerPrefs.save;

		//save Highscore
        	//int oldScore = PlayerPrefs.getInt("Score");
        	//int newScore = StaticSingleton.instant.getCollected();
        	//if (newScore > oldScore) PlayerPrefs.SetInt("Score", newScore);
		//}
}
